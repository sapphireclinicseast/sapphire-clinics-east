// Bumped on every deploy. The client compares its baked-in value against
// /api/version; a mismatch means the page is running an old build and it
// reloads itself to pick up the latest code. Increment this each deploy.
export const BUILD_VERSION = '2026-09-06-d'
